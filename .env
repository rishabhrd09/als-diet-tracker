# diet-tracker-app/.env - FOR LOCAL DEVELOPMENT ONLY
# DO NOT COMMIT THIS FILE TO GITHUB! Add it to .gitignore

# Use the NEW secret key you generated for development too, or another random one.
# Avoid using the old compromised key anywhere.
#SECRET_KEY='your_NEW_randomly_generated_secret_key_here' 
SECRET_KEY='8)p(t2-)vut@$29+-1m9bof*2+5g%j+gxoj8!nu*c-3y6=s6f1'
 

# Local development settings
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# Local database (if you want to explicitly set it, otherwise settings.py falls back to sqlite)
# DATABASE_URL=sqlite:///db.sqlite3

# Local CORS/CSRF settings
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
CSRF_TRUSTED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Add any other API keys or sensitive settings here for local dev